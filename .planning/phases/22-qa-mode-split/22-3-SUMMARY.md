# Plan 22-3 Summary
Build scripts updated in CI (`VITEPRESS_ENV=author npm run docs:build`) to explicitly create separate `dist-public` and `dist-author` folders for production and author environments.
