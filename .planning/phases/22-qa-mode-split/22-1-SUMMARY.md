# Plan 22-1 Summary
Environment variables `VITEPRESS_ENV=author` introduced in `config.mjs` to toggle authoring specific settings like `lineNumbers` and QA directories, eliminating the need for a separate config file.
